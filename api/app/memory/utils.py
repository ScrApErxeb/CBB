# app/memory/utils.py

# Exemple de fonction utilitaire pour formater une mémoire avant de l'afficher
def format_memory_content(content: str) -> str:
    """Fonction pour formater le contenu de la mémoire, par exemple, pour l'affichage"""
    return content.strip().capitalize()
